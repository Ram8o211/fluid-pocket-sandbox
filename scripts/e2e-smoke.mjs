import { spawn } from 'node:child_process';import { setTimeout as sleep } from 'node:timers/promises';
const server=spawn(process.execPath,['scripts/dev-server.mjs'],{stdio:'ignore'});try{await sleep(250);const r=await fetch('http://127.0.0.1:4173/');if(!r.ok)throw new Error(`HTTP ${r.status}`);const html=await r.text();for(const marker of ['scene','scene2d','view2dBtn','view3dBtn','materialSelect','brushToolBtn','eraserToolBtn','inspectToolBtn','cameraToolBtn','toolSize','sheetToggleBtn','motionBtn','manualPad','graphicsPreset','renderRepresentation','combustionTemperature','combustionRate','desktopHelp','saveInspectionBtn','exportBtn'])if(!html.includes(marker))throw new Error(`Missing UI marker ${marker}`);console.log('E2E static smoke OK: app shell and critical controls reachable');}finally{server.kill('SIGTERM');}
