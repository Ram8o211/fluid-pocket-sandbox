import { performance } from 'node:perf_hooks';import { SimulationEngine } from '../src/simulation/SimulationEngine.mjs';import { generateMaterial } from '../src/materials/MaterialGenerator.mjs';
for(const count of [500,1000,1500]){const sim=new SimulationEngine(Math.max(1800,count));const m=generateMaterial(77,1);sim.registerMaterial(m);while(sim.ps.count<count)sim.emit(1,{x:(sim.ps.count%20)*.08-0.8,y:2-Math.floor(sim.ps.count/20)*.025,z:((sim.ps.count/20)|0)%10*.07-.35},Math.min(24,count-sim.ps.count),.4);for(let i=0;i<10;i++)sim.step(1/40);const t=performance.now();for(let i=0;i<40;i++)sim.step(1/40);const ms=(performance.now()-t)/40;console.log(`${count} particles: ${ms.toFixed(2)} ms/sim-step`);}
